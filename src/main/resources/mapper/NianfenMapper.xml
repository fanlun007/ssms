<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.fanlun.dao.NianfenDao">
    <select id="queryNianfen" resultType="java.lang.Integer">
        SELECT DISTINCT year FROM sg ORDER BY year ASC
    </select>
</mapper>